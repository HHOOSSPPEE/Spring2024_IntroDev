/*
//pickup items
function item_add(_item)
    {

        var _added = false;

    if array_length(obj_inventory.inv) < obj_inventory.inv_max
    {
        array_push(obj_inventory.inv, _item);
        _added = true;
    }

    return _added;
    }
*/