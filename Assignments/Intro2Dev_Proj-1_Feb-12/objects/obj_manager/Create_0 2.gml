/*
game_w = 256;
game_h = 240;

screen_width = display_get_width();
screen_height = display_get_height();

scale_factor_w = floor(screen_width / game_w);
scale_factor_h = floor(screen_height / game_h);
scale_factor = min(scale_factor_w, scale_factor_h);

new_w = game_w * scale_factor;
new_h = game_h * scale_factor;

view_set_wport(view_camera[0], new_w);
view_set_hport(view_camera[0], new_h);

*/

pipe_location = 928;